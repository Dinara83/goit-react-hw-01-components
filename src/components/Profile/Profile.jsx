// import PropTypes from 'prop-types';
import './Profile.module.css'

const Profile = () => {
    // const { url, username, tag, location, avatar, stats } = props;
    return (
      <div className="profile">profile
    </div>
    );
  }

  export default Profile;
